{
  "name": "@libs/orchestrator",
  "version": "0.0.1",
  "private": true,
  "description": "Internal orchestrator library for Quiz2Biz AI multi-agent system.",
  "main": "src/index.ts",
  "scripts": {
    "test": "npx jest --config jest.config.js --detectOpenHandles --forceExit"
  },
  "dependencies": {
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@anthropic-ai/tokenizer": "^0.0.4",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "tiktoken": "^1.0.22",
    "word-count": "^0.3.1"
  }
}