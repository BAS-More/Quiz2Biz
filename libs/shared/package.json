{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared library - DTOs, interfaces, utilities",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx swc src -d dist --strip-leading-paths --config-file ../../.swcrc && npx tsc --emitDeclarationOnly",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "dependencies": {
    "@nestjs/swagger": "^7.3.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
